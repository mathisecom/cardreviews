{% comment %}
  ביקורות לקוחות ביקורתi
  Grille desktop + slider tactile mobile, sans bibliothèque externe.
{% endcomment %}

<section
  id="avisi-reviews-{{ section.id }}"
  class="avisi-reviews"
  lang="he"
  dir="rtl"
  style="
    --section-bg: {{ section.settings.section_bg }};
    --card-bg: {{ section.settings.card_bg }};
    --accent: {{ section.settings.accent }};
    --title: {{ section.settings.title_color }};
    --text: {{ section.settings.text_color }};
    --muted: {{ section.settings.muted_color }};
    --border: {{ section.settings.border_color }};
    --radius: {{ section.settings.radius }}px;
    --gap: {{ section.settings.gap }}px;
    --max-width: {{ section.settings.max_width }}px;
  "
>
  <div class="avisi-reviews__inner">
    <header class="avisi-reviews__header">
      <div class="avisi-reviews__heading">
        {% if section.settings.eyebrow != blank %}
          <p class="avisi-reviews__eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {% endif %}

        {% if section.settings.heading != blank %}
          <h2>{{ section.settings.heading | escape }}</h2>
        {% endif %}

        {% if section.settings.intro != blank %}
          <div class="avisi-reviews__intro rte">{{ section.settings.intro }}</div>
        {% endif %}
      </div>

      {% if section.settings.show_summary %}
        <div class="avisi-reviews__summary">
          <div>
            <strong>{{ section.settings.summary_title | escape }}</strong>
            <div class="avisi-reviews__stars" aria-label="{{ section.settings.summary_rating }} מתוך 5 כוכבים">
              {% for i in (1..5) %}
                <span class="{% if i <= section.settings.summary_rating %}is-active{% endif %}" aria-hidden="true">★</span>
              {% endfor %}
            </div>
          </div>

          <i aria-hidden="true"></i>

          <div class="avisi-reviews__summary-copy">
            <span>{{ section.settings.summary_prefix | escape }}</span>
            <strong>{{ section.settings.summary_count | escape }}</strong>
            <span>{{ section.settings.summary_source | escape }}</span>
          </div>
        </div>
      {% endif %}
    </header>

    {% if section.blocks.size > 0 %}
      <div class="avisi-reviews__slider">
        <div
          class="avisi-reviews__track"
          data-track
          role="region"
          aria-label="{{ section.settings.slider_label | escape }}"
          tabindex="0"
        >
          {% for block in section.blocks %}
            <article class="avisi-reviews__card" data-card {{ block.shopify_attributes }}>
              <div>
                <div class="avisi-reviews__stars" aria-label="{{ block.settings.rating }} מתוך 5 כוכבים">
                  {% for i in (1..5) %}
                    <span class="{% if i <= block.settings.rating %}is-active{% endif %}" aria-hidden="true">★</span>
                  {% endfor %}
                </div>

                {% if block.settings.review != blank %}
                  <blockquote>{{ block.settings.review | escape | newline_to_br }}</blockquote>
                {% endif %}
              </div>

              <div class="avisi-reviews__author">
                {% if block.settings.photo != blank %}
                  <div class="avisi-reviews__avatar">
                    {{
                      block.settings.photo
                      | image_url: width: 120
                      | image_tag: loading: 'lazy', widths: '60, 90, 120', sizes: '48px', alt: block.settings.name
                    }}
                  </div>
                {% else %}
                  <div
                    class="avisi-reviews__avatar"
                    style="--avatar-bg: {{ block.settings.avatar_bg }}; --avatar-color: {{ block.settings.avatar_color }};"
                    aria-hidden="true"
                  >
                    {{ block.settings.initial | default: block.settings.name | slice: 0 | upcase | escape }}
                  </div>
                {% endif %}

                <div class="avisi-reviews__author-copy">
                  <strong>{{ block.settings.name | escape }}</strong>
                  {% if block.settings.date != blank %}
                    <span>{{ block.settings.date | escape }}</span>
                  {% endif %}
                  {% if block.settings.verified %}
                    <span class="avisi-reviews__verified">✓ {{ section.settings.verified_text | escape }}</span>
                  {% endif %}
                </div>
              </div>
            </article>
          {% endfor %}
        </div>

        {% if section.blocks.size > 1 %}
          <div class="avisi-reviews__controls">
            <button type="button" data-prev aria-label="{{ section.settings.prev_label | escape }}">←</button>

            <div class="avisi-reviews__dots">
              {% for block in section.blocks %}
                <button
                  type="button"
                  data-dot="{{ forloop.index0 }}"
                  aria-label="הצגת ביקורת {{ forloop.index }}"
                  class="{% if forloop.first %}is-active{% endif %}"
                  {% if forloop.first %}aria-current="true"{% endif %}
                ></button>
              {% endfor %}
            </div>

            <button type="button" data-next aria-label="{{ section.settings.next_label | escape }}">→</button>
          </div>
        {% endif %}
      </div>
    {% elsif request.design_mode %}
      <p class="avisi-reviews__empty">הוסיפו בלוקים מסוג „ביקורת לקוח” בעורך התבנית.</p>
    {% endif %}
  </div>
</section>

{% style %}
  #avisi-reviews-{{ section.id }} {
    background: var(--section-bg);
    color: var(--text);
    direction: rtl;
    text-align: right;
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
  }

  #avisi-reviews-{{ section.id }} * {
    box-sizing: border-box;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__inner {
    width: min(calc(100% - 40px), var(--max-width));
    margin: 0 auto;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__header {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: end;
    gap: 32px;
    margin-bottom: {{ section.settings.header_gap }}px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__heading {
    max-width: {{ section.settings.heading_width }}px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__eyebrow {
    margin: 0 0 14px;
    color: var(--accent);
    font-size: {{ section.settings.eyebrow_size }}px;
    font-weight: 800;
    line-height: 1.2;
    letter-spacing: .16em;
    text-transform: uppercase;
  }

  #avisi-reviews-{{ section.id }} h2 {
    margin: 0;
    color: var(--title);
    font-size: clamp({{ section.settings.heading_mobile }}px, 3.1vw, {{ section.settings.heading_desktop }}px);
    font-weight: {{ section.settings.heading_weight }};
    line-height: 1.08;
    letter-spacing: -.03em;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__intro {
    margin-top: 15px;
    color: var(--muted);
    font-size: {{ section.settings.intro_size }}px;
    line-height: 1.6;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__intro > * {
    margin: 0;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary {
    display: flex;
    align-items: center;
    gap: 18px;
    min-width: 285px;
    padding: 18px 22px;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: {{ section.settings.summary_radius }}px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary > div:first-child {
    display: grid;
    gap: 4px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary > div:first-child > strong {
    color: var(--title);
    font-size: 20px;
    line-height: 1.1;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary i {
    width: 1px;
    height: 40px;
    background: var(--border);
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary-copy {
    display: grid;
    gap: 2px;
    color: var(--muted);
    font-size: 12px;
    line-height: 1.25;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__summary-copy strong {
    color: var(--title);
    font-size: 13px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__track {
    display: grid;
    direction: ltr;
    grid-template-columns: repeat({{ section.settings.columns }}, minmax(0, 1fr));
    gap: var(--gap);
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-width: 0;
    min-height: {{ section.settings.card_height }}px;
    padding: {{ section.settings.card_padding }}px;
    background: var(--card-bg);
    border: {{ section.settings.border_width }}px solid var(--border);
    border-radius: var(--radius);
    box-shadow: 0 10px 30px rgba(17, 24, 39, {{ section.settings.shadow_opacity | divided_by: 100.0 }});
    direction: rtl;
    text-align: right;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__stars {
    display: flex;
    gap: 1px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__stars span {
    color: {{ section.settings.star_inactive }};
    font-size: {{ section.settings.star_size }}px;
    line-height: 1;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__stars span.is-active {
    color: var(--accent);
  }

  #avisi-reviews-{{ section.id }} blockquote {
    margin: 18px 0 30px;
    color: var(--text);
    font-size: {{ section.settings.review_size }}px;
    font-style: normal;
    font-weight: {{ section.settings.review_weight }};
    line-height: 1.55;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__author {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: auto;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__avatar {
    display: grid;
    flex: 0 0 {{ section.settings.avatar_size }}px;
    width: {{ section.settings.avatar_size }}px;
    height: {{ section.settings.avatar_size }}px;
    place-items: center;
    overflow: hidden;
    background: var(--avatar-bg, var(--accent));
    border-radius: 50%;
    color: var(--avatar-color, #fff);
    font-size: {{ section.settings.avatar_font }}px;
    font-weight: 800;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__author-copy {
    display: grid;
    min-width: 0;
    gap: 2px;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__author-copy strong {
    overflow: hidden;
    color: var(--title);
    font-size: {{ section.settings.author_size }}px;
    line-height: 1.25;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__author-copy span {
    color: var(--muted);
    font-size: {{ section.settings.meta_size }}px;
    line-height: 1.25;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__author-copy .avisi-reviews__verified {
    color: var(--accent);
    font-weight: 700;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__controls {
    display: none;
  }

  #avisi-reviews-{{ section.id }} .avisi-reviews__empty {
    padding: 30px;
    border: 1px dashed var(--border);
    border-radius: var(--radius);
    color: var(--muted);
    text-align: center;
  }

  @media (max-width: 989px) {
    #avisi-reviews-{{ section.id }} .avisi-reviews__header {
      grid-template-columns: 1fr;
      align-items: start;
      gap: 24px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__summary {
      width: fit-content;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__track {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 749px) {
    #avisi-reviews-{{ section.id }} {
      padding: {{ section.settings.padding_top_mobile }}px 0 {{ section.settings.padding_bottom_mobile }}px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__inner {
      width: min(calc(100% - 28px), var(--max-width));
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__header {
      margin-bottom: {{ section.settings.header_gap_mobile }}px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__summary {
      width: 100%;
      min-width: 0;
      justify-content: space-between;
      padding: 16px 18px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__slider {
      margin-right: -14px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__track {
      display: flex;
      gap: {{ section.settings.mobile_gap }}px;
      overflow-x: auto;
      padding: 0 14px 8px 0;
      scroll-behavior: smooth;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
      -webkit-overflow-scrolling: touch;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__track::-webkit-scrollbar {
      display: none;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__card {
      flex: 0 0 {{ section.settings.mobile_width }}%;
      min-height: {{ section.settings.card_height_mobile }}px;
      padding: {{ section.settings.card_padding_mobile }}px;
      scroll-snap-align: start;
    }

    #avisi-reviews-{{ section.id }} blockquote {
      font-size: {{ section.settings.review_size_mobile }}px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__controls {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      padding: 14px 14px 0 0;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__controls > button {
      display: grid;
      flex: 0 0 44px;
      width: 44px;
      height: 44px;
      place-items: center;
      padding: 0;
      background: var(--accent);
      border: 1px solid var(--accent);
      border-radius: 50%;
      color: #fff;
      cursor: pointer;
      font: inherit;
      font-size: 21px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__controls > button:disabled {
      cursor: not-allowed;
      opacity: .35;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__dots {
      display: flex;
      flex: 1;
      justify-content: center;
      gap: 8px;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__dots button {
      width: 8px;
      height: 8px;
      padding: 0;
      background: var(--border);
      border: 0;
      border-radius: 99px;
      cursor: pointer;
      transition: width .2s ease, background .2s ease;
    }

    #avisi-reviews-{{ section.id }} .avisi-reviews__dots button.is-active {
      width: 24px;
      background: var(--accent);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #avisi-reviews-{{ section.id }} .avisi-reviews__track {
      scroll-behavior: auto;
    }
  }
{% endstyle %}

<script>
  (() => {
    const section = document.getElementById('avisi-reviews-{{ section.id }}');
    if (!section || section.dataset.ready === 'true') return;
    section.dataset.ready = 'true';

    const track = section.querySelector('[data-track]');
    const cards = [...section.querySelectorAll('[data-card]')];
    const prev = section.querySelector('[data-prev]');
    const next = section.querySelector('[data-next]');
    const dots = [...section.querySelectorAll('[data-dot]')];

    if (!track || cards.length < 2) return;

    let active = 0;
    let frame;
    const mobile = () => matchMedia('(max-width: 749px)').matches;

    const offset = index => cards[index] ? cards[index].offsetLeft - track.offsetLeft : 0;

    const update = index => {
      active = Math.max(0, Math.min(index, cards.length - 1));

      dots.forEach((dot, i) => {
        const selected = i === active;
        dot.classList.toggle('is-active', selected);
        selected ? dot.setAttribute('aria-current', 'true') : dot.removeAttribute('aria-current');
      });

      if (prev) prev.disabled = active === 0;
      if (next) next.disabled = active === cards.length - 1;
    };

    const go = index => {
      if (!mobile()) return;
      const target = Math.max(0, Math.min(index, cards.length - 1));
      track.scrollTo({
        left: offset(target),
        behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth'
      });
      update(target);
    };

    const sync = () => {
      if (!mobile()) return;
      cancelAnimationFrame(frame);
      frame = requestAnimationFrame(() => {
        let nearest = 0;
        let distance = Infinity;

        cards.forEach((card, index) => {
          const currentDistance = Math.abs(offset(index) - track.scrollLeft);
          if (currentDistance < distance) {
            distance = currentDistance;
            nearest = index;
          }
        });

        update(nearest);
      });
    };

    prev?.addEventListener('click', () => go(active - 1));
    next?.addEventListener('click', () => go(active + 1));
    dots.forEach(dot => dot.addEventListener('click', () => go(Number(dot.dataset.dot))));
    track.addEventListener('scroll', sync, { passive: true });

    addEventListener('resize', () => {
      if (mobile()) sync();
      else {
        track.scrollLeft = 0;
        update(0);
      }
    });

    update(0);
  })();
</script>

{% schema %}
{
  "name": "ביקורות לקוחות ביקורתi",
  "tag": "section",
  "class": "section-avisi-reviews",
  "max_blocks": 12,
  "settings": [
    { "type": "header", "content": "תוכן" },
    { "type": "text", "id": "eyebrow", "label": "כותרת עליונה", "default": "ביקורות לקוחות" },
    { "type": "text", "id": "heading", "label": "כותרת", "default": "שירות שעושה את ההבדל." },
    { "type": "richtext", "id": "intro", "label": "טקסט פתיחה" },
    { "type": "checkbox", "id": "show_summary", "label": "הצגת סיכום הביקורות", "default": true },
    { "type": "text", "id": "summary_title", "label": "כותרת הסיכום", "default": "מצוין" },
    { "type": "range", "id": "summary_rating", "label": "דירוג כולל", "min": 1, "max": 5, "step": 1, "default": 5 },
    { "type": "text", "id": "summary_prefix", "label": "טקסט לפני מספר הביקורות", "default": "מבוסס על" },
    { "type": "text", "id": "summary_count", "label": "מספר ביקורות", "default": "77 ביקורות" },
    { "type": "text", "id": "summary_source", "label": "מקור", "default": "לקוחות" },

    { "type": "header", "content": "צבעים" },
    { "type": "color", "id": "section_bg", "label": "רקע המקטע", "default": "#FFFFFF" },
    { "type": "color", "id": "card_bg", "label": "רקע הכרטיסים", "default": "#F9FAFB" },
    { "type": "color", "id": "accent", "label": "צבע ראשי", "default": "#0066FF" },
    { "type": "color", "id": "title_color", "label": "כותרות", "default": "#101218" },
    { "type": "color", "id": "text_color", "label": "טקסט", "default": "#363A44" },
    { "type": "color", "id": "muted_color", "label": "טקסט משני", "default": "#747A89" },
    { "type": "color", "id": "border_color", "label": "מסגרות", "default": "#E5EAF2" },
    { "type": "color", "id": "star_inactive", "label": "כוכבים לא פעילים", "default": "#D8DEE8" },

    { "type": "header", "content": "פריסה" },
    { "type": "range", "id": "max_width", "label": "רוחב מרבי", "min": 900, "max": 1600, "step": 20, "unit": "px", "default": 1320 },
    { "type": "range", "id": "columns", "label": "מספר עמודות במחשב", "min": 2, "max": 4, "step": 1, "default": 3 },
    { "type": "range", "id": "gap", "label": "מרווח בין הכרטיסים", "min": 8, "max": 48, "step": 2, "unit": "px", "default": 20 },
    { "type": "range", "id": "heading_width", "label": "רוחב מרבי של הכותרת", "min": 400, "max": 1100, "step": 20, "unit": "px", "default": 820 },
    { "type": "range", "id": "card_height", "label": "גובה מינימלי של הכרטיסים", "min": 220, "max": 500, "step": 10, "unit": "px", "default": 280 },
    { "type": "range", "id": "card_padding", "label": "ריווח פנימי של הכרטיסים", "min": 16, "max": 56, "step": 2, "unit": "px", "default": 28 },
    { "type": "range", "id": "radius", "label": "עיגול פינות הכרטיסים", "min": 0, "max": 40, "step": 2, "unit": "px", "default": 22 },
    { "type": "range", "id": "summary_radius", "label": "עיגול פינות הסיכום", "min": 0, "max": 32, "step": 2, "unit": "px", "default": 16 },
    { "type": "range", "id": "border_width", "label": "עובי המסגרות", "min": 0, "max": 3, "step": 1, "unit": "px", "default": 1 },
    { "type": "range", "id": "shadow_opacity", "label": "שקיפות הצל", "min": 0, "max": 25, "step": 1, "unit": "%", "default": 6 },

    { "type": "header", "content": "טיפוגרפיה" },
    { "type": "range", "id": "eyebrow_size", "label": "גודל הכותרת העליונה", "min": 10, "max": 20, "step": 1, "unit": "px", "default": 12 },
    { "type": "range", "id": "heading_desktop", "label": "גודל כותרת במחשב", "min": 28, "max": 76, "step": 2, "unit": "px", "default": 46 },
    { "type": "range", "id": "heading_mobile", "label": "גודל כותרת בנייד", "min": 24, "max": 48, "step": 1, "unit": "px", "default": 34 },
    {
      "type": "select",
      "id": "heading_weight",
      "label": "משקל הכותרת",
      "default": "700",
      "options": [
        { "value": "500", "label": "בינוני" },
        { "value": "600", "label": "חצי מודגש" },
        { "value": "700", "label": "מודגש" },
        { "value": "800", "label": "מודגש מאוד" }
      ]
    },
    { "type": "range", "id": "intro_size", "label": "גודל טקסט הפתיחה", "min": 13, "max": 22, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "star_size", "label": "גודל הכוכבים", "min": 12, "max": 28, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "review_size", "label": "גודל הביקורת במחשב", "min": 14, "max": 24, "step": 1, "unit": "px", "default": 17 },
    { "type": "range", "id": "review_size_mobile", "label": "גודל הביקורת בנייד", "min": 14, "max": 22, "step": 1, "unit": "px", "default": 16 },
    {
      "type": "select",
      "id": "review_weight",
      "label": "משקל טקסט הביקורות",
      "default": "400",
      "options": [
        { "value": "400", "label": "רגיל" },
        { "value": "500", "label": "בינוני" },
        { "value": "600", "label": "חצי מודגש" }
      ]
    },
    { "type": "range", "id": "author_size", "label": "גודל שם הלקוח", "min": 12, "max": 20, "step": 1, "unit": "px", "default": 15 },
    { "type": "range", "id": "meta_size", "label": "גודל התאריך", "min": 10, "max": 17, "step": 1, "unit": "px", "default": 12 },
    { "type": "range", "id": "avatar_size", "label": "גודל האווטאר", "min": 36, "max": 72, "step": 2, "unit": "px", "default": 46 },
    { "type": "range", "id": "avatar_font", "label": "גודל האות באווטאר", "min": 12, "max": 26, "step": 1, "unit": "px", "default": 16 },

    { "type": "header", "content": "ריווחים" },
    { "type": "range", "id": "padding_top", "label": "ריווח עליון במחשב", "min": 0, "max": 160, "step": 4, "unit": "px", "default": 80 },
    { "type": "range", "id": "padding_bottom", "label": "ריווח תחתון במחשב", "min": 0, "max": 160, "step": 4, "unit": "px", "default": 80 },
    { "type": "range", "id": "header_gap", "label": "רווח מתחת לכותרת", "min": 20, "max": 100, "step": 4, "unit": "px", "default": 48 },
    { "type": "range", "id": "padding_top_mobile", "label": "ריווח עליון בנייד", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 56 },
    { "type": "range", "id": "padding_bottom_mobile", "label": "ריווח תחתון בנייד", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 56 },
    { "type": "range", "id": "header_gap_mobile", "label": "רווח מתחת לכותרת בנייד", "min": 16, "max": 72, "step": 4, "unit": "px", "default": 32 },

    { "type": "header", "content": "סליידר בנייד" },
    { "type": "range", "id": "mobile_width", "label": "רוחב כרטיס", "min": 78, "max": 100, "step": 1, "unit": "%", "default": 88 },
    { "type": "range", "id": "mobile_gap", "label": "מרווח בין הכרטיסים", "min": 8, "max": 28, "step": 2, "unit": "px", "default": 14 },
    { "type": "range", "id": "card_height_mobile", "label": "גובה מינימלי של הכרטיסים", "min": 220, "max": 500, "step": 10, "unit": "px", "default": 280 },
    { "type": "range", "id": "card_padding_mobile", "label": "ריווח פנימי של הכרטיסים", "min": 16, "max": 40, "step": 2, "unit": "px", "default": 24 },
    { "type": "text", "id": "slider_label", "label": "תיאור נגיש לסליידר", "default": "ביקורות הלקוחות שלנו" },
    { "type": "text", "id": "prev_label", "label": "כפתור לביקורת הקודמת", "default": "ביקורת קודמת" },
    { "type": "text", "id": "next_label", "label": "כפתור לביקורת הבאה", "default": "ביקורת הבאה" },
    { "type": "text", "id": "verified_text", "label": "טקסט ביקורת מאומתת", "default": "ביקורת מאומתת" }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "ביקורת לקוח",
      "settings": [
        { "type": "range", "id": "rating", "label": "דירוג", "min": 1, "max": 5, "step": 1, "default": 5 },
        { "type": "textarea", "id": "review", "label": "ביקורת", "default": "שירות לקוחות מהיר ויעיל במיוחד! תודה שפתרתם את הבעיה שלי בזמן קצר כל כך." },
        { "type": "text", "id": "name", "label": "שם הלקוח", "default": "סמנתה היולין" },
        { "type": "text", "id": "date", "label": "תאריך", "default": "04/09/2025" },
        { "type": "checkbox", "id": "verified", "label": "הצגת ביקורת מאומתת", "default": false },
        { "type": "image_picker", "id": "photo", "label": "תמונת הלקוח" },
        { "type": "text", "id": "initial", "label": "אות ראשונה", "default": "ס" },
        { "type": "color", "id": "avatar_bg", "label": "רקע האווטאר", "default": "#0066FF" },
        { "type": "color", "id": "avatar_color", "label": "צבע האות באווטאר", "default": "#FFFFFF" }
      ]
    }
  ],
  "presets": [
    {
      "name": "ביקורות לקוחות ביקורתi",
      "blocks": [
        {
          "type": "review",
          "settings": {
            "rating": 5,
            "review": "שירות לקוחות מהיר ויעיל במיוחד! תודה שפתרתם את הבעיה שלי בזמן קצר כל כך.",
            "name": "סמנתה היולין",
            "date": "04/09/2025",
            "initial": "ס"
          }
        },
        {
          "type": "review",
          "settings": {
            "rating": 5,
            "review": "תודה גדולה לצוות על ליווי מקצועי ללא פשרות. הכול היה ברור, פשוט ומהיר.",
            "name": "בומין פריירה",
            "date": "02/09/2025",
            "initial": "ב"
          }
        },
        {
          "type": "review",
          "settings": {
            "rating": 5,
            "review": "מושלם, מהיר ואיכותי. אני מרוצה מאוד מהרכישה שלי, תודה!",
            "name": "קאבאווין רוניאק",
            "date": "02/09/2025",
            "initial": "ק"
          }
        }
      ]
    }
  ]
}
{% endschema %}